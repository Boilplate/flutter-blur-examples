// ClipRect around Sizedbox
// positioned

import 'package:flutter/material.dart';

class PartScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
